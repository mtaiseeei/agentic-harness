# v0.5.4 release validation

- Date: 2026-09-11
- Verdict: PASS; no blocking findings.
- Evaluator: independent role `/root/evaluator_grilling`.
- Baseline: git HEAD `2579d715d13beef7767cc30c3eb10af607ecd932`; evaluated the current working tree before release operations.

## Scope

This incremental validation covers the English-only, model-neutral configuration comments, corrected runtime guidance using inherited/configured defaults, matching regression assertions, and version synchronization to `0.5.4`. The accepted grilling behavior evaluation remains in [Sprint 001 Patch 001 feedback](../feedback/sprint-001-patch-001.md); those unchanged behavioral probes were not repeated.

## Independent checks

| Check | Result |
|---|---|
| `node scripts/check-positioning.mjs` | exit 0; 14 checks passed |
| `node scripts/check-loop-rules.mjs` | exit 0; 13 surfaces verified, including bundled grilling integrity |
| `node plugins/harness/scripts/check-runtime-config.mjs` | exit 0; 56 checks passed |
| `python3 -m json.tool` on both marketplace catalogs and both plugin manifests | all exit 0 |
| `claude plugin validate plugins/harness` | exit 0; Validation passed |
| `git diff --check` | exit 0 |

Using the bundled `plugins/harness/vendor/smol-toml/index.cjs` parser, loaded each file from `git show HEAD:<path>` and the working tree, then compared their parsed objects with Node `assert.deepEqual`:

- `.harness/config.toml`: exact parsed-object equality; all settings preserved, including the existing deprecated compatibility key.
- `plugins/harness/templates/.harness/config.toml`: exact parsed-object equality; all settings preserved.
- Both files' comment lines passed ASCII-only and no concrete model-ID/name assertions. Runtime defaults, lifecycle, escalation thresholds, and loop-stop limits were not changed by the comment cleanup.

Independently parsed the release manifests and asserted all four version fields equal `0.5.4`: Claude marketplace metadata, its harness entry, Claude plugin manifest, and Codex plugin manifest. The Codex marketplace has no separate version field to update.

Reviewed the actual additional loop guidance and assertion changes. The guidance now describes inherited defaults and configured standard/strong values, while retaining resolver-directed fallback and launch-evidence requirements. The runtime implementation is unchanged; the successful suite includes inheritance, explicit settings, fallback, no-overwrite initialization, and read-only resolver coverage.

## Boundaries and self-review

- No `product` or `verification-infra` findings in this incremental review.
- This is source/release validation, not evidence that a tag or release was published, a Marketplace snapshot was refreshed, a downstream repository was updated, or Mac mini installations were upgraded. Those operations require their own actual results.
- Prior grilling evidence remains bounded to the recorded simulated inputs and observed independent role outputs; no new all-host live guarantee is claimed.
- Evaluator wrote only this report; no source, configuration, state, specification, or prior feedback files were edited. Results and PASS agree; no new behavioral probes or verification infrastructure were introduced.
